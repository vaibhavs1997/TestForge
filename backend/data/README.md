# Local data directory

Runtime JSON stores are written here per project. This folder is gitignored except for this README.

To reset local state, stop the backend and delete project subfolders under `backend/data/`, then restart and re-import contracts from the UI.
