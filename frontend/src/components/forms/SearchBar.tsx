// Re-export the shared SearchBar implementation (single source of truth).
export { SearchBar, type SearchBarProps } from '../shared/SearchBar';
export { SearchBar as default } from '../shared/SearchBar';
