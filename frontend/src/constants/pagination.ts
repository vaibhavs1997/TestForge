/**
 * Frontend Pagination Constants
 * Default pagination sizes and related configuration
 */

/** Default page size for list views and tables */
export const DEFAULT_PAGE_SIZE = 25;

/** Available page size options for pagination */
export const PAGE_SIZE_OPTIONS = [25, 50, 100];

/** Default page size for row editor pagination */
export const ROW_EDITOR_PAGE_SIZE = 10;

/** Row height in pixels for virtualized tables */
export const VIRTUALIZED_TABLE_ROW_HEIGHT = 50;
